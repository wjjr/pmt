
#include "ukkonen.h"

#include "../error.h"

unsigned char ukkonen_search(const struct algorithm_context *algorithm_context) {
    die(EXIT_MISTAKE, 0, "uk: not implemented", algorithm_context->files_count);
    return 255;
}
