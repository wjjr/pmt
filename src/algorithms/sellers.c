
#include "sellers.h"

#include "../error.h"

unsigned char sellers_search(const struct algorithm_context *algorithm_context) {
    die(EXIT_MISTAKE, 0, "se: not implemented", algorithm_context->files_count);
    return 255;
}
