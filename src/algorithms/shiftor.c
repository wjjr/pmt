
#include "shiftor.h"

#include "../error.h"

unsigned char shiftor_search(const struct algorithm_context *algorithm_context) {
    die(EXIT_MISTAKE, 0, "so: not implemented", algorithm_context->files_count);
    return 255;
}
